# Copy to .env on the server or set in Apache / PHP-FPM (not committed).
# Local XAMPP defaults match below if unset in config.php.

DB_HOST=localhost
DB_NAME=techon_erp_network
DB_USER=root
DB_PASS=

# Production HTTPS redirect (leave unset for local dev)
# ENV=production

# CORS: comma-separated exact origins; private LAN / localhost allowed by default patterns.
# TECHON_ALLOWED_ORIGINS=https://app.example.com,http://localhost:5173
# TECHON_CORS_ALLOW_NULL_ORIGIN=1

# Safe access log (no tokens/secrets): logs/api.log (rotates at 5MB), or absolute path outside web root:
# TECHON_ENABLE_LOGS=1
# TECHON_API_LOG_FILE=/var/log/techon/api.log

# Verbose internal logs (e.g. ping DB errors) — development only
# TECHON_DEBUG=1

# Trust X-Forwarded-For for client IP (behind reverse proxy only)
# TECHON_TRUST_PROXY=1

# Dev-only open API without tc_api_key.php:
# TECHON_ERP_OPEN_API=1
