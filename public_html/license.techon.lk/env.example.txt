# License API — set in Apache / nginx / PHP-FPM environment (not committed).
# Same value must be set on Electron clients: LICENSE_SECRET or TC_LIC_SERVER_SECRET.

LICENSE_SECRET=your-strong-shared-secret

# Backward compatible alias:
# TC_LIC_SERVER_SECRET=your-strong-shared-secret

# Admin panel: bcrypt hash (preferred) OR SQLite via bootstrap_admin.php
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD_HASH=$2y$10$...

# Optional .env in this directory is loaded by env_load.php before license checks.

# Production HTTPS redirect
# ENV=production

# Safe API log: logs/api.log — or absolute path outside docroot:
# TECHON_ENABLE_LOGS=1
# TECHON_API_LOG_FILE=/var/log/techon/api.log

# Verbose internal error_log (dev only)
# TECHON_DEBUG=1
