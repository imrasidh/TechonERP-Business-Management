DB_HOST=localhost
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# Production HTTPS redirect (omit on localhost)
# ENV=production

# CORS
# TECHON_ALLOWED_ORIGINS=https://app.techon.lk,https://erp.techon.lk
# TECHON_CORS_ALLOW_NULL_ORIGIN=1

# Safe API log: logs/api.log (path + IP + status only; rotates at 5MB), or:
# TECHON_ENABLE_LOGS=1
# TECHON_API_LOG_FILE=/var/log/techon/api.log

# Verbose internal error_log detail (dev only)
# TECHON_DEBUG=1

# Behind reverse proxy: trust X-Forwarded-For for logging / rate context
# TECHON_TRUST_PROXY=1
